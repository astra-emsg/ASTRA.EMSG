<UserControl x:Class="ASTRA.EMSG.Mobile.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <Grid>
            <ProgressBar Height="25" Margin="20" MinWidth="200" MaxWidth="500" Minimum="0" Maximum="100" Value="{Binding ProgressValue}"></ProgressBar>
            <TextBlock Text="{Binding ProgressText}" HorizontalAlignment="Center"></TextBlock>
        </Grid>
    </Grid>
</UserControl>
