@using Resources
@using System.Linq
@model List<string>
           
@if (Model != null && Model.Any())
{
    <span>@NotificationLocalization.MandantLogoUploadErrorTitle</span>
    <br />
    <br />
    foreach (var error in Model)
    {
        <span style="color: red">@error</span><br />
    }
    <br />    
    <span>@NotificationLocalization.MandantLogoUploadErrorHelp</span>
}
else
{
    <span>@NotificationLocalization.MandantLogoUploadSuccessful</span>
}